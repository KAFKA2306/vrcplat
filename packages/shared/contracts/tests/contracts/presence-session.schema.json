{
  "$ref": "#/definitions/presence-session",
  "definitions": {
    "presence-session": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "format": "uuid"
        },
        "worldId": {
          "type": "string",
          "minLength": 1
        },
        "instanceId": {
          "type": "string",
          "minLength": 1
        },
        "enteredAt": {
          "type": "string",
          "format": "date-time"
        },
        "leftAt": {
          "type": "string",
          "format": "date-time"
        },
        "source": {
          "type": "string",
          "enum": [
            "beacon",
            "import"
          ]
        },
        "visibility": {
          "type": "string",
          "enum": [
            "private",
            "friends",
            "consented"
          ],
          "default": "private"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "worldId",
        "instanceId",
        "enteredAt",
        "source",
        "createdAt",
        "updatedAt"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
